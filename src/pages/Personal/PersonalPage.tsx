import React, { FC } from 'react';

import { Wrapper } from '../../components';

export const PersonalPage: FC = () => (
  <Wrapper pageName="Портфель">
    {/* _______props.children___________ */}
    Это будет личный кабинет
  </Wrapper>
);
